import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://willaur.github.io/olap-benchmarks/', // exact URL with trailing slash
  base: '/olap-benchmarks/',                         // leading & trailing slash
});
