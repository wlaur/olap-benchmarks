import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://wlaur.github.io/olap-benchmarks/', // exact URL with trailing slash
  base: '/olap-benchmarks/',                         // leading & trailing slash
});
