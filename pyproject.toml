[project]
name = "tsdb-benchmarks"
version = "0.1.0"
description = "Time series database benchmarks"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=1.16.1",
    "ruff>=0.12.0",
]
